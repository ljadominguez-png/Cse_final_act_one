sample output:
choose what encoding scheme: (Unicode[1], utf-8[2], utf-16[3])
- hexa or binary
 - if the input is binary i shexa, then convert it to binary

- Unicode input utf - 8 and utf- 16 <- output

logics
Binary 0 and 1
Hexadecimal = 0123456789ABCDEF


Both binary and hexa has 0 and 1

Problem How can the program detect if the input is hexadecimal or binary

Instead of array use charAt
